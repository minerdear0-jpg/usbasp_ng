# Hardware checks that do not need ATmega328P.
#
# Bench names:
#   no-dot       — USBasp clone with NG classic; the programmer (USB to host, -c usbasp)
#   yellow-dot   — experiment target
#                  ISP mode: on the header, J2 closed, no-dot programmes it
#                  USB mode: yellow-dot itself on the host (lsusb, descriptors, avrdude -c usbasp
#                  as the device under test, HIDUART enumeration). J2 open.
#
# USB mode (yellow-dot on host, J2 open, unplug no-dot):
#   host/usb-inspect-usbasp.sh
#   host/usbasp-getcaps.py
#
# Classic L0 (measured): 16c0:05dc, bcdUSB 1.10, device class 0xFF,
#   1 interface, 0 endpoints, iSerial 0, no HID/BOS.
# HIDUART (measured, yellow-dot, iSerial YEL0):
#   USB 2.01, 3 interfaces, BOS/MS OS 2.0, GETCAPABILITIES 01 00 00 01
#   ISP read of no-dot through composite: signature/flash OK (do not write no-dot)
#   UART loopback: TQFP 30–31 (PD0–PD1), host/usbasp-hiduart-loopback.py

#
# ISP mode (yellow-dot on header, J2 closed, no-dot as programmer):
#   avrdude -c usbasp -p atmega8
#   avrdude -c usbasp -p atmega8 -U signature:r:-:h
#   avrdude -c usbasp -p atmega8 -U flash:r:/tmp/yellow-dot.bin:r
#   avrdude -c usbasp -p atmega8            # AUTO should enable without -B
#   avrdude -c usbasp -p atmega8 -B 8       # slower SCK (SETISPSCK)
#   avrdude -c usbasp -p atmega8 -B 0.5     # 1.5 MHz; 3 MHz needs -B short enough (CAP_3MHZ)
#
# Pass: zero exit, vid=0x16c0 pid=0x05dc, signature 0x1e 0x93 0x07 for mega8.
# Measured fuses on yellow-dot: lfuse 0xef hfuse 0xd9. Do not write 0xc9 blindly.
#
# no-dot programmer: usbisp-sized classic (~4018 B). JP3 on that board is not
# sampled (HAS_SCK_JUMPER=0). Clone profile (4024+) reads PC2 for 8 kHz.
#
# SCK smoke (yellow NG as programmer, no-dot as ISP target, signature only):
#   AUTO, -B 0.5 (1.5 MHz), -B 8 (93.75 kHz): PASS on both HIDUART and NG classic.
#   -B 22 / 50 / 250 (32 / 16 / 4 kHz software SCK): FAIL ENABLEPROG 0x01 on both.
#   Conclusion: software SCK programming-enable is a real functional bug on this
#   pair; it is not HIDUART/composite IRQ. Root cause (SCK vs MOSI vs MISO vs RST)
#   not scoped yet. USB stayed enumerated after FAIL on classic.
#   Parked: do not iterate sck.c until a scope capture of -B 22 vs -B 8.
#
# Next ISP (hardware SPI only, no -B 22/50/250):
#   EEPROM read of no-dot (yellow programmer, J2 closed on target):
#     avrdude -c usbasp -p atmega8 -U eeprom:r:/tmp/no-dot.eep:r
#   Do not eeprom:w / flash:w / fuse:w on no-dot.
#   EEPROM write+verify: yellow as target, no-dot as programmer, J2 closed:
#     avrdude -c usbasp -p atmega8 -U eeprom:w:<tiny.bin>:r
#     avrdude -c usbasp -p atmega8 -U eeprom:v:<tiny.bin>:r
#   3 MHz (CAP_3MHZ): yellow programmer, -B shorter than 0.5, signature only:
#     avrdude -c usbasp -p atmega8 -B 0.25 -v -U signature:r:-:h
#   JP3/PC2 8 kHz: yellow programmer, jumper closed, AUTO signature; SCK must
#     stay ~8 kHz for the whole CONNECT (must not ramp back to 1.5 MHz).
#
# Measured 2026-08-25 (no-dot USB programmer, yellow ISP target, J2 closed):
#   signature 1E 93 07, lfuse 0xef, hfuse 0xd9
#   EEPROM read 512 B (erased 0xFF); write+verify 16 B marker then restore 0xFF: PASS
#     (yellow as mega8 EEPROM target; WRITEEEPROM firmware under test is no-dot)
#   SETISPSCK from no-dot: -B 8 → 93750 Hz, -B 0.5 → 1.5 MHz, -B 0.25 → 3 MHz: PASS
#   NG 3 MHz / JP3 / NG EEPROM host path: need yellow on USB (programmer).
#
# Measured 2026-08-25 (yellow NG USB programmer, no-dot ISP target, J2 closed):
#   signature 1E 93 07, lfuse 0xef, hfuse 0xd9 (same as yellow)
#   NG READEEPROM: 512 B, erased 0xFF. No eeprom:w / flash:w / fuse:w on no-dot.
#   NG SETISPSCK: -B 8 → 93750 Hz, -B 0.5 → 1.5 MHz, -B 0.25 → 3 MHz: PASS
#   JP3 still open (AUTO did not stay at 8 kHz). Close jumper on yellow to test.
#
# Measured 2026-08-25 JP3 closed (yellow NG programmer, no-dot target):
#   AUTO and -B 0.5: ENABLEPROG 0x01. Avrdude still prints 1.5 MHz on -B 0.5
#   (SETISPSCK reply) but CONNECT pins USBASP_ISP_SCK_8 (~8 kHz software).
#   Same SW-SCK programming-enable bug as -B 22/50/250. JP3 cannot pass until
#   software SCK works. Open jumper to restore HW SPI.
#
# Measured 2026-08-26 JP3 LED (HIDUART yellow USB, no avrdude required):
#   Jumper closed: PC0 breathes (8 kHz applied immediately). Open: 1 Hz USB idle.
#   Host SETISPSCK id is not overwritten by the jumper (open after CONNECT → 1 Hz).
#
# Measured 2026-08-26 (HIDUART 6894 B yellow USB programmer, no-dot ISP target):
#   GETCAPABILITIES 01 00 00 01. Signature 1E 93 07, lfuse 0xef, hfuse 0xd9.
#   Flash read 4018 B; EEPROM read 512 B all 0xFF. No write on no-dot.
#   SETISPSCK AUTO / -B 8 / -B 0.5 / -B 0.25 (3 MHz): PASS (JP3 open).
#   EEPROM rewrite SERIAL=YEL0 (10 B) as ISP target; USB iSerial YEL0 confirmed.
#   avrdude flash chip-erase wipes EEPROM (hfuse 0xd9, EESAVE off). make flash for
#   HIDUART now writes the .eep after the hex (do not parallel flash and eeprom).
#   UART loopback PD0–PD1 (TQFP 30–31, tweezers): HELLO echoed. Do not short pin 29 (RESET).
#
# Waveform: parked until a capture of -B 22 vs -B 8. Report: docs/SOFTWARE_SCK.md
#   Third mega8 sniffer or FX2 LA — host/isp-sniffer/README.md
#   (do not flash sniffer onto no-dot or onto yellow unless replacing NG).

